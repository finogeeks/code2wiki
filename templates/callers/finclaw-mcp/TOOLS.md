# TOOLS — casst MCP caller (example)

## Prefer

| Tool | When |
|------|------|
| `casst-mcp_ask` | Default product questions |
| `casst-mcp_explain` | Deeper explanations |
| `casst-mcp_generate` | Briefs / outlines |

## Avoid for knowledge answers

`web_search`, `web_fetch`, `http_request` — denied in this example’s
`tool-invocation-policy.yaml`. Rematerialize after edits; start a new chat.
