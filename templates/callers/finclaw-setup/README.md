# Example FinClaw profile: setup guide for a code2wiki site (not production).
#
# Materialize:
#   ./scripts/materialize-caller.sh finclaw-setup
#   FINCLAW_HOME=runtime/examples/finclaw-setup finclaw chat --profile casst-caller
